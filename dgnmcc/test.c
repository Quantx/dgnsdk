int a, b, * c, * g;
unsigned int d;

struct test {
    int a;
} tv;

int *** z;

char x, y;

float w;

//char f[];

int main( int argc, char ** argv )
{
    z = y || tv.a || a.bsd ;
//    printf( "%ld\n", sizeof(f) );

//    a->b->c.d;

//    a = (c + g)[ (a++ + -g[2]) * ++b ] + 4, 7 + b;


//      sizeof ( a + b + c );
//    ++(*a);
//    (c)++;
//    mystruct->g[a] = b++ == 4 ? c > a ? (a + 4) * sizeof b : 5 << b : -b && (c % 4);
//      a = b ? a : c ? a : b ? c : g;
}
